import cl from "./Header.module.css";

const Header = () => {
  return (
    <header className={cl.header_wrap}>
      <div className="container">Добавление продукции</div>
    </header>
  );
};

export default Header;
