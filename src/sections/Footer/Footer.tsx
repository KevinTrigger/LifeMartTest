import cl from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={cl.footer}>
      <div className="container">(c) ЖизньМарт</div>
    </footer>
  );
};

export default Footer;
